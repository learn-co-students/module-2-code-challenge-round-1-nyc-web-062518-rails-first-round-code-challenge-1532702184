<h2>All Heroines</h2>

<!-- <%= form_tag("/search", method: "get") do %>
  <h3><%= label_tag(:q, "Search superpower for heroines:") %><br></h3>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>
<% end %> -->

<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= link_to heroine.super_name, heroine %>
    </li>
    <br>
  <% end %>
</ul>
